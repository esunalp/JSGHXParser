//! Basis Value-enum waarin later componentresultaten terecht komen.

/// Waarden die tijdens evaluatie tussen componenten worden doorgegeven.
#[derive(Debug, Clone, PartialEq)]
pub enum Value {
    /// Nog niet geïmplementeerde placeholder.
    Empty,
}
